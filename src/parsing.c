#include "minirt.h"



void add_token(char *line, t_scene *scene)
{
	
}

bool is_valid_input(char *file, t_scene *scene) {
	int fd = /*open the file*/;
	while (/*the file*/) {
		char *line = get_next_line();
		//validate the line
		add_token(line, scene);
	}
}